// seller_list_widget.dart
import 'package:flutter/material.dart';

class SellerListWidget extends StatelessWidget {
  const SellerListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Padding(
        padding: EdgeInsets.all(16.0),
        child: Text("Seller List Widget (Placeholder)"),
      ),
    );
  }
}